{
  "_comment": "Default policy rules for the wasm-af demo. First-match-wins. Deny-by-default.",
  "rules": [
    {
      "_comment": "web-search may call summarizer directly (direct comms permitted).",
      "source": "wasm-af:web-search",
      "target": "wasm-af:summarizer",
      "capability": "agent-direct",
      "comms_mode": "direct"
    },
    {
      "_comment": "web-search may use outgoing HTTP (mediated — orchestrator creates the link).",
      "source": "wasm-af:web-search",
      "target": "*",
      "capability": "http",
      "comms_mode": "mediated"
    },
    {
      "_comment": "summarizer may call the LLM inference provider (mediated).",
      "source": "wasm-af:summarizer",
      "target": "*",
      "capability": "llm",
      "comms_mode": "mediated"
    }
  ]
}
