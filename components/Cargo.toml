[workspace]
resolver = "2"
members = [
    "agent-types",
    "policy-engine",
    "agents/web-search",
    "agents/summarizer",
    "agents/url-fetch",
]

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
